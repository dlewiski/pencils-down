const styles = (theme) => ({
  root: {
    padding: theme.spacing(5, 10, 5),
  },
  title: {
    fontSize: '40px',
    fontWeight: 'bold',
    paddingBottom: '20px',
  },
  textBlock: {
    padding: theme.spacing(1.5, 0),
  },
});

export default styles;